# Integration

- init (local)
- check ssh-keys
- create (use `gh` tool or git) repo on `github`
- add `remote repoo`
- set new branch `main`

- add file for watching
- commit(checks)
- push to `remote repo`

